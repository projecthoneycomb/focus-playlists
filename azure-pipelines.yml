# Docker
# Build a Docker image 
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - script: sudo /usr/local/bin/npm install @projecthoneycomb/focus-tools -g
      displayName: 'Install focus-tools'
    - script: mkdir tracks
      displayName: 'Make tracks folder'
    - script: manifest-to-files --i="./manifest.json" --o="./tracks"
      displayName: 'Resolve manifest'
    - task: Docker@2
      inputs:
        containerRegistry: 'Focus Mode Registry'
        repository: 'stream-lofi'
        command: 'buildAndPush'
        Dockerfile: '**/lofi/Dockerfile'
        tags: |
          $(Build.BuildId)
          latest